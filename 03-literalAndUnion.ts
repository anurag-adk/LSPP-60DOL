export type userRole = "admin" | "buyer" | "seller"; //exporting to import in other files
//This is called union type, it is like giving limited options to a variable type.

let user: userRole = "seller";
